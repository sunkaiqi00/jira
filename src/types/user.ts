export interface AuthForm {
  userName: string;
  password: string;
}

export interface UserParam {
  id: number;
  name: string;
  email: string;
  title: string;
  organization: string;
  token: string;
}
